spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: testdb

server:
  error:
    include-message: always

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2
mongock:
  migration-scan-package: 'ru.mikhailov.otus.task11.migration'
